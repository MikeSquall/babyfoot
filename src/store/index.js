import moduleLanguage from './lang';
import moduleTeams from './teams';
import moduleRanking from './ranking';
import modulePlayers from './players';
import moduleMatches from './matches';

export default {
  language: moduleLanguage,
  teams: moduleTeams,
  ranking: moduleRanking,
  players: modulePlayers,
  matches: moduleMatches,
};
